# Public dev API URL (optional). Leave empty to use Vite proxy to http://localhost:8080
# VITE_API_URL=http://localhost:8080
#
# Safe for frontend: public API base URL only.
# Never put database passwords, JWT signing secrets, or admin keys here.
